/**
 * DOM - Document Object Model
 * 
 * DOM stands for Document Object Model.  DOM is a programming interface for HTML and XML documents.
 * 
 * When the browser tries to render an HTML document, it creates an object based on the HTML document 
 * called DOM. Using this DOM, we can manipulate or change various elements inside the HTML document.
 */